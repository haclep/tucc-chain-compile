# environments — rebuilt from requirements.txt, never committed
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Psi4 exporter side
psi4_scratch/
timer.dat
*.clean

# generated numerics — regenerable, potentially large
*.npz
*.npy
*.h5
dumps/
checkpoints/

# editor / OS noise
.vscode/
.idea/
Thumbs.db

# pip editable-install metadata — regenerated by `pip install -e .`
src/*.egg-info/
build/
dist/

# resumable-compiler checkpoints — ship via GitHub Releases, not history
*.pkl

# console scratch
*.log
*.out

# manual backups — Git is the backup now
backup/